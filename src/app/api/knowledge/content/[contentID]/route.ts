import { NextRequest } from "next/server";

export async function GET(
  req: NextRequest,
  { params }: { params: { contentID: string } }
) {
  try {
  } catch (error) {
    console.log(error);
  }
}
